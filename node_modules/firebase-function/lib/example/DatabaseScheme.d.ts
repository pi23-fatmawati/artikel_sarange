import { type CryptedScheme } from '../src';
export type DatabaseScheme = {
    persons: {
        [PersonId in string]: CryptedScheme<{
            name: string;
            age: number;
        }>;
    };
    animals: {
        [AnimalId in string]: {
            name: string;
        };
    };
};
