import { type DatabaseType, type IDatabaseReference, type IFirebaseSchedule } from '../src';
import { type DatabaseScheme } from './DatabaseScheme';
export declare class PetAnimalsSchedule implements IFirebaseSchedule {
    private readonly databaseReference;
    constructor(databaseType: DatabaseType, databaseReference: IDatabaseReference<DatabaseScheme>);
    execute(): Promise<void>;
    private pet;
}
