import * as functions from 'firebase-functions';
import { type DatabaseType } from './DatabaseType';
export type FirebaseScheduleConstructor = new (databaseType: DatabaseType) => FirebaseSchedule;
export interface FirebaseSchedule {
    executeFunction(): Promise<void>;
}
export declare namespace FirebaseSchedule {
    function create(schedule: string, createFirebaseSchedule: (context: functions.EventContext<Record<string, string>>) => FirebaseSchedule): functions.CloudFunction<unknown>;
}
